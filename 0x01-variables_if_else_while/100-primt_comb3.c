#include <stdio.h>

/* The following function print all possible double digit combination */

int main(void)
{
	for (int i = 0; i < 10; i++)
	{
		for (int x = 
		putchar(i);
		putchar(44);
	}
	return (0);
}
