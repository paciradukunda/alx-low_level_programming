#include "main.h"
/**
  * @brief function adds a new node to the head of linked list. 
  * 
  * @head: the head of the linked list.
  * @str: the data for new node.
  *
  * Return: node address
  */
list_t *add_node(list_t **head, const char *str)
{
	struct node* tmp = malloc(sizeof(list_t));
	struct node* new = malloc(sizeof(list_t));
	char dup = malloc(sizeof

	tmp = *head;

        new->data = data;
	new->link = first;
	first = new;

	return (first);

}
