#include "main.h"

/**
  * print_square - prints square of length size
  *
  * @size - integer input
  *
  * Return: void
  */

void print_square(int n)
{
	int i, x;

	if (size != 0 && size !< 0)
	{
		for (i = 0; i <= size; i++)
		{
			for (x = 0; x <= size; x++)
			{
				_putchar('#');
			}
		}
	}
	_putchar('\n');
}
