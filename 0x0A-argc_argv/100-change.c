#include "main.h"
/**
  * main - prints the given ammount into coins
  * @argc: count for the given parameters
  * @argv: array of parameters
  *
  * RETURN: o if successfull
  */
int main(int argc, char *argv[])
{
	char num[] = {"25", "10", "5", "2", "1"};
	int coin;

	if(argc != 2)
	{
		printf("Error\n");
		return (1);
	}
	else if(atoi(argv[1]) < 0)
	{
		printf("%d\n", 0);
	}
	else
	{
		for(i = 0; i < 5; i++)
		{
			while(1 == 1)
			{
				if(atoi
				balance = atoi(argv[1]) - atoi(num[i]);


}

