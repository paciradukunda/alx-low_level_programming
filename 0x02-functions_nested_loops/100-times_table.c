#include "main.h"
/**
  * print_times_table - prints n times tables
  *
  * Return: no return
  */

void print_times_table(int n)
{
	int i,x;

	for (i = 0; i <= n; i++)
	{
		for (x = 0; x <= n; x++)
		{
			if (x < n)
			else
		}
		_putchar("\n");
	}
	return (0);
}
