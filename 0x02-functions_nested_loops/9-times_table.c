#include "main.h"
/**
  * times_table - prints 9 times tables
  *
  * Return: 0 if successfull
  */

int times_table(void)
{
	int i,x;

	for (i = 0; i < 10; i++)
	{
		for (x = 0; x < 10; x++)
		{
			if (x < 9)
			else
		}
		_putchar("\n");
	}
	return (0);
}
